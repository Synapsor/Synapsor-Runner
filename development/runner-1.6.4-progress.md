# Runner 1.6.4 progress

## Objective

Implement and verify `/home/sandesh-tiwari/Desktop/C++/goal.txt` against the
current `@synapsor/runner@1.6.3` baseline.

## Status

Complete locally on `fix/runner-1.6.4-validation-dx`. External merge, push,
publish, tag, deployment, and deprecation actions remain intentionally pending
owner authorization.

## Baseline

- Branch: `fix/runner-1.6.4-validation-dx`
- Starting commit: `382d171`
- Starting worktree: clean
- Expected local versions before recheck:
  - Runner: `1.6.3`
  - Spec: `1.6.0`
  - DSL: `1.6.0`
- Active evidence:
  - `/home/sandesh-tiwari/Desktop/C++/bug.md`
  - `/home/sandesh-tiwari/Desktop/C++/RESULTS.md`

## Confirmed scope

- Secure verified non-browser activation for the exact Scoped Explore boundary.
- Predictable conventional nested config discovery.
- Multi-resource review-state correctness and blocker navigation.
- Conservative financial/payment sensitive-field classification.
- Modern, plain-language, accessible Workbench.
- Deterministic explainable cross-domain generation.
- Operational and legacy compatibility verification.
- Two independent packed-artifact clean-room UI/CLI/MCP labs.
- A hard-gated reviewed star-schema phase only if both labs prove real
  multi-hop demand; it is the first whole-phase deferral candidate and may not
  ship partially.
- An optional, local-only BYOM Ask surface for OpenAI, Anthropic, and explicitly
  configured OpenAI-compatible providers, with exact MCP/runtime authority
  parity, no persisted provider secrets or conversations, direct egress
  disclosure, hardened endpoint handling, real Workbench interaction, mock
  provider coverage, and one owner-authorized live OpenAI acceptance run.
- Runner `1.6.4` release preparation and complete documentation.

## Progress

- [x] Read the current goal, bug report, and results.
- [x] Confirmed clean `main` at `382d171`.
- [x] Created `fix/runner-1.6.4-validation-dx`.
- [x] Verify package/registry baselines.
- [x] Run baseline tests and packed-artifact gates.
- [x] Build the finding-to-code/test compatibility matrix.
- [x] Replace order-dependent review checkboxes with revisioned,
  input-subdigest-bound confirmations.
- [x] Preserve unrelated confirmations through resource regeneration and schema
  rescan; reserve full deletion for explicit Start over.
- [x] Reject stale-tab review saves.
- [x] Classify `card_on_file` and payment-card aliases conservatively without
  treating ordinary `payment_status`, `panel_position`, or `pan_size_cm` as
  sensitive.
- [x] Add bounded conventional `./synapsor/synapsor.runner.json` discovery with
  ambiguity refusal and config-relative storage resolution.
- [x] Add complete CLI review-bundle export and replay-resistant, role-verified
  signed headless activation through the canonical activation core.
- [x] Finish the first Workbench correctness, starter-catalog, and
  plain-language pass.
- [x] Make candidate review requirements follow the authority actually retained
  in the candidate, while accepting the exact legacy full-decision submission.
- [x] Add deterministic ranked scope evidence and compile existing public DSL
  as an input to generation.
- [x] Add bounded business labels and suggested questions to the authoring-only
  data-description surface without changing its authority.
- [x] Add cross-domain deterministic scope, sensitivity, analytics, and
  structured-evidence fixtures across PostgreSQL/MySQL and
  Prisma/Drizzle/OpenAPI.
- [x] Make the no-model-key Workbench PM composer expose only reviewed
  one-hop FK relationships and emit the same typed relationship plans that the
  runtime validates.
- [x] Build and run the first packed community-solar clean-room slice through
  fresh schema inspection, real browser review/activation, a scoped exact-row
  read, a relationship-backed PM aggregate, and Protect activation.
- [x] Extend that packed journey through a generated reversible UPDATE,
  exact-digest action activation, proposal-only invocation, a live source
  freshness check, human approval outside MCP, guarded PostgreSQL apply, and
  no-ID receipt/replay inspection.
- [x] Add additive reviewed star and depth-two relationship authority with
  exact catalog proof, explicit nullable-link semantics, per-relation scope,
  counted-entity results, and dependency-scoped drift invalidation.
- [x] Verify numerically exact star/depth-two aggregates and deliberate
  one-to-many refusal against live PostgreSQL and MySQL.
- [x] Measure bounded relationship-review burden on a realistic 40-table
  schema: two starter relationship decisions, 19 total starter decisions,
  with deterministic per-resource proposal caps.
- [x] Prove the demand-driven exact-path review journey from the packed
  artifact: refusal with catalog evidence, human staging, digest activation,
  retry, and successful answer in three interactions.
- [x] Add and live-test the reviewed database-view escape hatch for derived
  measures without adding model-facing formulas or expressions.
- [x] Implement Phase 3I BYOM Ask without adding authority or weakening the
  complete no-model composer.
- [x] Run deterministic Anthropic/OpenAI-compatible provider gates, both
  clean-room Ask UI journeys, exact secret scans, and the authorized live
  OpenAI Workbench acceptance test.
- [x] Implement phases 1-8 from the goal.

## Decisions

- `--operator <id> --yes` alone is not sufficient activation authority.
- Existing upward project discovery is retained; the missing conventional case
  is `./synapsor/synapsor.runner.json`.
- Review correctness precedes visual redesign.
- Workbench, CLI, DSL, and canonical JSON must share one authority/state model.
- A clean-room failure reopens implementation and requires both labs to rerun.
- Phase 6 relationship expansion cannot begin until dependency-scoped review
  state and both labs pass. Plans may select only pre-reviewed FK-proven paths;
  they never improvise joins or expressions.

## Finding-to-code/test matrix

| Finding | Primary code | Regression evidence |
| --- | --- | --- |
| Financial fields missed | `packages/schema-inspector/src/sensitivity.ts` | `sensitivity.test.ts` positive and false-positive matrix |
| Regeneration clears all review | `apps/runner/src/local-ui.ts`, `auto-boundary.ts` | two-resource A->B and B->A Workbench API tests |
| Browser clears confirmations | `apps/runner/src/boundary-workbench.ts` | persisted response/reload plus both packed clean-room browser journeys |
| Opaque final blocker | `apps/runner/src/boundary-workbench.ts` | exact outstanding-decision response and final browser blocker-navigation matrix |
| Stale Workbench tabs overwrite | `apps/runner/src/local-ui.ts` | revision mismatch returns HTTP 409 |
| Nested config not discovered | `apps/runner/src/project-resolution.ts` | 7-test bounded discovery/ambiguity/storage matrix passes |
| No secure headless activation | `apps/runner/src/cli.ts`, canonical activation core, proposal ledger | signed exact-bundle success plus wrong-role, replay, and tamper denials pass |
| Workbench jargon/density | `boundary-workbench.ts` and activity shell | final 1.6.4 visual matrix passes across desktop/mobile and operational states |
| BYOM Ask missing | new local Workbench client plane over the existing model-facing registry/runtime | mock and live provider, parity, secret, UI, and both clean-room gates pass |
| Whole catalog overwhelms first review | `local-ui.ts`, `boundary-workbench.ts` | fresh 40-table Workbench presents a deterministic three-resource starter pack; full catalog remains explicit |
| Omitted resources still require review | `auto-boundary.ts`, `local-ui.ts` | candidate-specific decision normalization and legacy exact-full-decision compatibility pass |
| Scope names treated as authority | `auto-boundary.ts` | structural evidence ranking plus eight-domain cross-dialect denial matrix passes |
| PM composer hides reviewed relationships | `scoped-explore.ts`, `boundary-workbench.ts` | reviewed target fields/operators/measures only; relationship-backed suggestion and typed-plan tests pass |
| Protect selected an older row query after a newer aggregate | `scoped-explore.ts`, `boundary-workbench.ts` | protected results are newest-first and Workbench selects the exact visible protect token |
| Tenant-only aggregate dimension failed beside a principal-scoped fact | `protect-query.ts`, `packages/mcp-server/src/index.ts` | activation and live RLS preflight require principal settings only on relations that declare principal scope; tenant scope remains mandatory on every relation |
| Guided action omitted approval freshness | `guided-action.ts`, `local-ui.ts` | generated UPDATE/DELETE actions require target-row freshness; exact required role and `fresh` status render in Workbench |
| External proposal contract made freshness policy look unknown | `packages/config/src/index.ts`, `apps/runner/src/cli.test.ts` | unresolved cross-file references defer until contract merge, then config validation and doctor enforce the fully resolved catalog |
| Long-page CDP click races smooth scrolling | `scripts/demo-video/cdp-client.mjs` | packed browser lifecycle clicks the intended approval/apply controls after instant scrolling |

## Tests

- `corepack pnpm test`
  - PASS: 55 files, 845 tests.
  - PASS: license/content.
  - PASS: DSL source paths.
  - PASS: Cursor plugin verification.
  - Duration: 178.50s.
- `corepack pnpm test:guided-onboarding:packed`
  - PASS on the current tree after repairing two first-screen copy regressions
    and updating the verifier for the review-by-exception starter pack.
  - PASS: packed FitFlow, 40 inspected resources and a bounded three-resource
    initial review.
  - Schema summary: 3.329s; first safe read: 5.863s; PM aggregate: 8.482s;
    protected capability: 9.889s; first guided proposal: 20.952s; complete
    guarded/operator matrix: 149.329s.
  - One shell command, zero manual file edits, and no external documentation
    through the first proposal.
  - PASS: all four approval/execution combinations, OIDC negatives, stale
    conflict, compensation, notifications, CLI/Workbench/stdio authority.
  - PASS: exact reviewer role plus 13 negative OIDC/JWKS cases, separate apply
    role after key rotation, zero persisted bearer tokens, tamper detection,
    two competing workers applying once, no immediate success notifications,
    blocked metadata SSRF, and verified notification-only replay.
- `corepack pnpm test:auto-boundary-visual`
  - PASS after the Workbench redesign: 14 captures across
    desktop/mobile/light/dark/loading/blocked/stale/error/attention states and
    a 40-table schema.
  - Fresh review presents three recommended resources; explicit Show all
    renders all 40; the catalog toggle is reversible without reusing a
    bootstrap token.
- `corepack pnpm exec vitest run apps/runner/src/auto-boundary.test.ts apps/runner/src/local-ui.test.ts apps/runner/src/scoped-explore.test.ts`
  - PASS: 42 tests after deterministic evidence ranking, candidate-scoped
    review decisions, starter selection, and bounded business metadata.
- `corepack pnpm exec vitest run apps/runner/src/auto-boundary-domains.test.ts apps/runner/src/auto-boundary.test.ts packages/schema-inspector/src/sensitivity.test.ts`
  - PASS: 56 tests across SaaS, retail, logistics, property management,
    healthcare, fintech, support, and industrial IoT fixtures.
  - PASS: PostgreSQL/MySQL plus Prisma/Drizzle/OpenAPI evidence; ambiguous and
    name-only tenant/principal candidates remain blocked.
- `corepack pnpm exec vitest run apps/runner/src/boundary-workbench.test.ts apps/runner/src/scoped-explore.test.ts`
  - PASS: 10 tests after adding relationship-aware business labels, bounded
    target fields/operators/measures, and typed one-hop Workbench plans.
- `corepack pnpm exec vitest run apps/runner/src/local-ui.test.ts -t "preserves unrelated review confirmations"`
  - PASS: both resource review orders reach activation; only dependent
    confirmations are invalidated.
- `corepack pnpm exec vitest run packages/schema-inspector/src/sensitivity.test.ts`
  - PASS: 32 deterministic sensitivity tests.
- `corepack pnpm exec vitest run apps/runner/src/boundary-cli.test.ts apps/runner/src/project-resolution.test.ts packages/schema-inspector/src/sensitivity.test.ts`
  - PASS: 40 tests, including signed exact-bundle activation, tamper/replay
    refusal, bounded nested-config discovery, and sensitivity regression.
- `corepack pnpm --filter @synapsor/runner build`
  - PASS after the review-state, CLI activation, project discovery, and initial
    Workbench redesign changes.
- `corepack pnpm test:clean-room:community-solar`
  - PASS from a fresh packed install, PostgreSQL database, project directory,
    ledger, and Chrome profile.
  - One shell command and no manual file edits.
  - Latest complete slice: schema summary 11.617s; boundary activation 12.968s;
    safe read 13.425s; relationship aggregate 13.997s; protected capability
    15.990s; first guided proposal 17.192s; human-approved guarded apply
    25.409s; total 31.351s.
  - PASS: exact `solar_maintenance_reviewer` projection, live freshness,
    proposal/approval non-mutation, guarded status/version transition, receipt,
    replay, and no-ID ledger timeline.
  - PASS: official MCP SDK parity; same-tenant other-principal and cross-tenant
    rows hidden; kept-out/filter/group/count-distinct, model-selected scope,
    raw SQL, arbitrary-function, unreviewed/fan-out path, and bound-overflow
    plans refused.
  - PASS: exact aggregate Protect token, generated DSL/canonical equivalence,
    authoring Explore shutdown, production protected-capability survival, and
    final `config validate`/`doctor` contract resolution.
  - Screenshots and machine-readable evidence are under
    `development/runner-1.6.4-community-solar-visual/` and
    `development/runner-1.6.4-community-solar-results.json`.
- `corepack pnpm exec vitest run apps/runner/src/scoped-explore.test.ts apps/runner/src/auto-boundary.test.ts apps/runner/src/workbench-candidate.test.ts`
  - PASS: 23 tests after improving deterministic suggested-question copy and
    keeping business-label fallbacks out of self-referential questions.
- `corepack pnpm exec vitest run packages/config/src/index.test.ts apps/runner/src/cli.test.ts --testNamePattern='defers freshness capability references|validates freshness policies against capabilities loaded from contract files'`
  - PASS: external-contract freshness references validate only after resolution;
    the same generated wiring passes the local doctor config gate.
- Preserved Community Solar live project:
  - PASS: `config validate --json`.
  - PASS: `doctor --json` with the real separated reader/writer roles, exact
    generation lock, protected aggregate, guided proposal, and PostgreSQL RLS.
  - The complete clean-room run still must be repeated from an empty directory
    after these fixes; this preserved-project check is diagnostic evidence, not
    a replacement for that release gate.
- `corepack pnpm test:reviewed-relationships`
  - PASS on live PostgreSQL and MySQL.
  - PASS: direct store plus depth-two product/category star grouping returns
    exact expected sums from one counted sales-fact entity.
  - PASS: nullable foreign keys require an explicit keep-null or exclude
    decision and both choices produce the expected totals.
  - PASS: tenant scope is enforced on every participating relation and
  cross-tenant dimension rows remain unavailable.
  - PASS: a reverse one-to-many line-item relationship is refused instead of
    silently multiplying facts.
  - Fixed MySQL metadata normalization so `IS_NULLABLE`, `DATA_TYPE`, defaults,
    and schema names use stable aliases; added MySQL `INT` to deterministic
    numeric-measure classification.
- `corepack pnpm test:auto-boundary-scale`
  - PASS: 40 resources; six total generated relationship candidates; no
    resource exceeded the bounded direct/depth-two proposal caps.
  - PASS: the three-resource starter pack adds two relationship decisions to
    17 baseline safety decisions (19 total).
  - PASS: activated runtime `tools/list` remains 7,345 bytes / approximately
    1,837 tokens.
- `node scripts/verify-packed-auto-boundary-explore.mjs`
  - PASS: the packed current Runner loads the exact candidate and activation
    digest saved by published `@synapsor/runner@1.6.3` without regeneration or
    authority drift.
  - PASS: an inactive but catalog-proven path appears as review-required
    metadata but cannot execute.
  - PASS: exact catalog evidence -> human stage -> exact digest activation ->
    retry succeeds in three interactions and 4.534s.
  - PASS: the widest current plan keeps trusted tenant and principal predicates
    on the counted relation and every reviewed relationship; production
    advertises only the protected named capability after Explore is disabled.
- `corepack pnpm test:packed-backward-compatibility`
  - PASS against published Runner `1.6.3`, DSL `1.6.0`, and Spec `1.6.0`.
  - PASS: unchanged protected single-hop DSL compiles byte-identically under
    published and current packed artifacts; the legacy singular relationship
    shape and digest remain unchanged.
- `corepack pnpm exec vitest run apps/runner/src/scoped-explore.test.ts`
  - PASS: 13 tests, including the widest three-relationship/four-relation
    PostgreSQL and MySQL plan, per-relation trusted scope, and suppression
    tightening across one, two, and three reviewed dimensions.
- `corepack pnpm --filter @synapsor-runner/postgres test`
  - PASS: 40 adapter tests, including hardened recursive attestation for
    security-invoker/security-barrier PostgreSQL views and refusal of ordinary
    owner-rights views.
- `node scripts/verify-packed-retail-clean-room.mjs`
  - PASS after adding the reviewed derived-measure recipe.
  - Strict public DSL compile and config validation run from the packed
    artifact.
  - Hardened PostgreSQL RLS serves
    `retail.average_net_revenue_retention_pacific` through a reviewed
    security-invoker view; exact result is 9,000 basis points, no member rows,
    and no source mutation.
  - Complete retail journey still passes in 39.201s with 45 inspected objects,
    review-by-exception, safe read, star aggregate, Protect, proposal, approval,
    guarded apply, and ledger inspection.
- `corepack pnpm exec vitest run apps/runner/src/operator-identity.test.ts packages/proposal-store/src/index.test.ts apps/runner/src/local-ui.test.ts apps/runner/src/cli.test.ts --maxWorkers=1 --minWorkers=1 --testTimeout=20000 --hookTimeout=20000`
  - PASS: 4 files, 234 tests, run sequentially in 215.37s.
  - PASS: the two previously CI-sensitive signed approver/apply role tests
    complete without timeout or leaked working-directory state.
  - PASS: exact-digest dual worker opt-in, policy reservations, fenced leases,
    pause/drain/retry/dead-letter/reconciliation state, hardened writer posture,
    stale authority and freshness refusal, tenant-scoped Attention Inbox, and
    verified operator decisions.
- Final pre-Ask release checks on the prepared versions
  (`@synapsor/runner@1.6.4`, `@synapsor/spec@1.7.0`,
  `@synapsor/dsl@1.7.0`):
  - PASS: source publish manifest.
  - PASS: license/content after keeping the root and packaged README identical
    and below 1,500 words.
  - PASS: Cursor plugin `1.6.4`, official format, path-with-spaces install,
    idempotent copy, clean uninstall, preserved unrelated config, and no
    embedded secret/activation authority.
  - PASS: final Workbench visual matrix with 14 screenshots and 27 exercised
    states under `development/runner-1.6.4-visual/`.
  - PASS: final packed FitFlow journey; schema summary 3.529s, activation
    3.842s, safe read 6.187s, aggregate 9.098s, protected capability 10.496s,
    first proposal 22.028s, and full guarded/operator matrix 156.774s.
  - PASS: one shell command, zero manual edits, no documentation, all four
    approval/execution combinations, OIDC/JWKS negatives, two-worker
    idempotency, freshness refusal, compensation, quiet notifications, SSRF
    refusal, and CLI/Workbench/stdio authority parity.
- Phase 3I BYOM Ask implementation and focused verification:
  - Added bounded OpenAI, Anthropic, and custom OpenAI-compatible adapters,
    in-memory sessions, explicit digest-bound egress consent, direct provider
    transport, safe cancellation/clear, and redacted provider failures.
  - Added a shared Workbench MCP gateway over the official MCP SDK. It exposes
    only active named capabilities plus valid local authoring tools and routes
    every call through the existing runtime. Empty named-runtime surfaces are
    valid and no longer issue an unsupported `tools/list`.
  - Added strict endpoint controls: fixed official origins, HTTPS for remote
    providers, HTTP only on loopback, redirect refusal, pinned validated DNS,
    private/special-address refusal, bounded requests/responses/time/tool
    loops, and no automatic provider retry.
  - Added the optional Workbench Ask panel with explicit provider state,
    direct-egress disclosure, exact reviewed tools and authority digest,
    session-only transcript, starter questions, cancellation, clear/change
    model, escaped model prose, tool traces, and proposal-only warnings.
  - Corrected post-authoring Workbench state to distinguish active protected
    named tools from no authority. Replaced the dead-end “analysis above”
    instruction with direct named-tool guidance.
  - PASS: `model-ask.test.ts` (16), `ask-mcp-gateway.test.ts` (2),
    focused `local-ui.test.ts`, `boundary-workbench.test.ts`, TypeScript, and
    `test:workbench-ask` desktop/mobile browser interaction and visual checks.
- Packed Ask clean-room evidence:
  - PASS: Community Solar through packed Runner/Spec, live PostgreSQL, empty
    browser profile, real Workbench UI, deterministic loopback provider, exact
    official-MCP aggregate parity, proposal-only write, and no source mutation.
    The corrected full run completed in 37.928s before the live-provider
    supplement.
  - PASS: Retail through packed Runner/Spec and its independent 45-object
    PostgreSQL schema. Ask produced exact official-MCP revenue aggregate
    parity, visibly refused grouping by the kept-out private customer note,
    and created an inert fulfillment proposal for `order-010`. Protect,
    guarded apply, ledger inspection, and post-authoring protected tools also
    passed in 42.359s.
  - Visual evidence now includes dedicated aggregate, refusal, and
    proposal-only captures under both
    `development/runner-1.6.4-community-solar-visual/` and
    `development/runner-1.6.4-retail-visual/`.
- Owner-authorized live OpenAI gate:
  - Initial run found a real remote-provider defect: Node 22 requests custom
    DNS lookup results with `all: true`, while Runner returned the legacy
    single-address callback shape, causing `ERR_INVALID_IP_ADDRESS` before
    HTTPS. Fixed the pinned lookup to support both callback shapes and changed
    the real loopback transport test to use a hostname.
  - PASS: the authorized key was loaded only in memory from the owner-supplied
    env file; its value was never printed, passed as an argument, or copied
    into repository artifacts.
  - PASS: `gpt-5-mini` called `app.describe_data` and `app.explore_data` through
    the packed Workbench against live Community Solar PostgreSQL. The aggregate
    matched official MCP exactly, source mutation remained false, and the
    provider turn completed in 16.453s.
  - PASS: exact-key scans before and after covered project/ledger, screenshots,
    Chrome profile, Workbench output, process arguments, and evidence output.
    Browser storage remained empty and the exact question was absent from the
    project and browser profile after Clear.
  - Machine-readable live metadata is in the `live_openai` section of
    `development/runner-1.6.4-community-solar-results.json`; it contains only
    provider/model, tool sequence, usage, latency, parity, mutation state, and
    scan verdicts.
- Phase M1 instant-onboarding implementation and focused verification:
  - Fresh interactive `start` now accepts an exported `DATABASE_URL`, explicit
    consent to a bounded regular `.env.local`/`.env`, or a no-echo session
    paste. The value is held only in the current Runner/Workbench process and
    restored or removed when it exits.
  - Workbench offers one conservative development resource, no relationships,
    no writes, sensitive/uncertain fields kept out, one combined trusted-scope
    question, and one recorded digest-bound **Activate and read** gesture.
  - The endpoint reuses `activateExplorationBoundary` and
    `createScopedExploreRuntime`, rechecks schema/read-only role posture, and
    drives a real structured aggregate count. Tenant/principal values are not
    returned, persisted, or included in the digest.
  - Shared/production assertions refuse before activation and route to full
    review. Existing explicit, manual-project, JSON, noninteractive, and
    selector routes remain separate.
  - PASS: 36 focused instant candidate/input/Workbench tests plus TypeScript.
- Phase M3 command-surface implementation and focused verification:
  - Human docs and generated client configs now use each package's direct
    `npx -y @synapsor/<package> <command>` form for the initial no-install
    invocation. After installation, follow-on guidance uses the package binary
    without repeating npm acquisition. Automation scripts remain free to use
    explicit pinned forms when reproducibility requires them.
    `verify-human-command-surface.mjs` prevents package/binary double-name
    forms across every `@synapsor/*` package and version pins in primary guides.
  - PASS: the complete human-facing sweep, including the Cloud CLI README,
    contains no `npx -y -p @synapsor/... <binary>` command.
  - Opted-in human paths imply exported `DATABASE_URL`; explicit flags win.
    Cwd remains the project-root default and inspected engine/schema defaults
    remain intact.
  - Generated MCP configs use the direct scoped package binary. One
    suppressible graduation tip is recorded per project after the first
    interactive success.
  - Registry check on 2026-07-25 returned `E404` for the unscoped
    `synapsor-runner` package. The prepared wrapper has one binary and no
    runtime/authority logic.
  - PASS: packed local Spec + scoped Runner + alias install. The packed alias
    exact-depends on `@synapsor/runner@1.6.4` and
    `synapsor-runner --version` returned `1.6.4` in a clean npm project.
- Workbench completion-state, code-preview, and operations-layout verification:
  - Added a shared, deterministic Synapsor DSL tokenizer and escaped DOM
    renderer. Protected-read and guided-action previews now distinguish DSL
    keywords, user-defined identifiers, strings, numbers, comments, and
    punctuation without a CDN or HTML injection path. Their plain/copy text
    remains byte-identical to the generated `.synapsor.sql`.
  - PASS: TypeScript and 32 focused Workbench/syntax tests. The browser visual
    harness passed 17 desktop/mobile captures and all 30 modeled states.
  - Fixed the instant-onboarding success view so trusted scope inputs disappear,
    active authority and source-mutation state are accurate, only one primary
    next action remains, and mobile anchor navigation does not hide content
    under the sticky header.
  - Replaced the three-column operations layout with a full-width activity and
    review workspace. Human approval and guarded apply remain separate,
    hash-bound operator decisions outside MCP.
- Repeated packed clean-room verification after the UI changes:
  - PASS: Community Solar through the packed tarballs and live PostgreSQL in
    51.841s. Highlighted protected/action DSL passed exact-source assertions in
    light and dark modes; human review and apply passed full-workspace alignment
    assertions. The full first-read, aggregate, Protect, proposal, approval,
    apply, receipt, replay, and Ask journey remained intact.
  - PASS: Retail through an independent packed 45-object schema in 58.163s,
    including its two-dimension reviewed star aggregate, view-recipe escape
    hatch, kept-out Ask refusal, guarded write, and the same DSL/layout checks.
  - The packed build also caught and fixed an omitted release asset:
    `docs/workbench-ask.md` is now included in the Runner tarball wherever its
    packaged README and docs link to it.
- Packed hidden-input clean-room rerun and retained-artifact inspection:
  - PASS: Community Solar through packed Runner/Spec and live PostgreSQL in
    39.331s after adding the final database-URL scans.
  - PASS: the public command received the URL through no-echo session input;
    the exact URL was absent from exported environment state, process
    arguments, terminal output, generated `.synapsor`/`synapsor` artifacts,
    Runner config, browser profile, screenshots, and machine-readable evidence.
  - PASS: schema summary 13.072s, activation 14.712s, first real scoped read
    15.178s, first PM aggregate 15.737s, protected capability 19.456s, proposal
    20.651s, and guarded apply 30.353s.
  - Retained the temporary project long enough to inspect the exact generated
    context/read/protected/action DSL, canonical contracts, generation lock,
    review report, config, seed rows, and scoped aggregate values. The
    disposable PostgreSQL container and volume were then removed.
- Documentation/current-state audit:
  - Root and packaged Runner READMEs remain byte-identical, under 1,500 words,
    and retain the audit/proof, supervised-worker, notification, security, and
    decision-guide gates.
  - Updated current feature guides to remove stale release-era freshness and
    reversibility wording, clarified selected `.env` consent, added local
    copy-exact Workbench DSL highlighting, and documented the direct-npx versus
    installed-command convention.
  - Updated prepared release notes/changelog and release policy with the
    hidden-input fast lane, command alias, full-width review layout,
    syntax-highlighted previews, four-package dependency order, and alias
    verification boundary.
  - PASS: human command-surface lint, license/content gate, and packaged Runner
    build/link check.
- Rebuilt
  `/home/sandesh-tiwari/Desktop/C++/SYNAPSOR_TECHNICAL_DEEP_DIVE.md` as a
  2,558-line current-system manual rather than a version chronology:
  - Covers packages/components, artifacts, Spec versus DSL compiler, all three
    authoring paths, DSL tokens versus author names, Context internals, config,
    stdio/HTTP/JWT security, tool generation, reads, scope, proposals,
    freshness, approval, guarded apply, receipt modes, CRUD/sets,
    compensation, app handlers, ledger/Workbench, workers, notifications,
    Auto Boundary, Explore, PM aggregates, relationships, Protect, Ask, Cloud,
    production checks, and honest limits.
  - Includes the actual packed Community Solar domain, relationship diagram,
    sample rows, Prisma/Drizzle/OpenAPI background, setup commands, generated
    DSL, canonical JSON, generation-lock explanation, exact scoped aggregate
    output, protected capability, guided action, write lifecycle, negative
    tests, measurements, and cleanup command.
  - PASS: ASCII-only text, balanced Markdown fences, no stale prepared/current
    version chronology, no verbose package/binary command form, and all local
    documentation links resolve.
- Final post-documentation and release verification:
  - Fixed the CLI quick-demo test isolation defect exposed under full-suite
    load. `demo --quick` now preserves the existing isolated `--state-dir`
    option when delegating to `try`; detailed, JSON, guided, and inspection
    tests no longer mutate global cwd or share one timeout. Four focused tests
    passed in 9.836s.
  - Fixed a stale case-sensitive packed-browser assertion after the Workbench
    copy changed from lowercase `active` to the exact visible state
    `Active reviewed boundary`. The activation API had already proved the
    boundary active; the end-to-end packed journey now checks the visible copy.
  - PASS: complete source suite, 62 files and 938 tests in 211.63s, followed by
    license/content, human command-surface, DSL source-path, and Cursor plugin
    verification.
  - PASS: packed alias at `1.6.4`; packed backward compatibility against
    published Runner `1.6.3`, DSL `1.6.0`, and Spec `1.6.0`.
  - PASS: packed Auto Boundary/Scoped Explore/Protect journey, including
    demand-driven relationship review, privacy suppression, differencing and
    hard budgets, absent production Explore tools, and retained protected named
    capability.
  - PASS: packed FitFlow 40-resource onboarding. One shell command, zero manual
    edits, safe read, PM aggregate, Protect, proposal, OIDC approval/apply,
    policy auto-approval, competing supervised workers, notifications,
    receipts/replay, and compensation all completed.
  - PASS: reviewed star/depth-two relationship tests on live PostgreSQL and
    MySQL; 17 Workbench screenshots and 30 modeled states; Workbench Ask
    desktop/mobile browser interaction with no persisted provider key or
    browser storage.
  - PASS: live PostgreSQL and MySQL guarded CRUD, bounded sets, reversibility,
    approval/apply freshness, trusted principal scope, contract conformance,
    and live PostgreSQL database-enforced scope.
  - PASS: complete two-Runner fleet gate, including shared-ledger locking,
    rate limits, quorum, batch apply, competing workers, crash recovery at
    before/during/after-commit boundaries, dead letters, bounded pool pressure,
    backup, restore, and retention.
  - PASS: generated MCP client configs plus current Claude Code `2.1.220` and
    Codex CLI `0.145.0` acceptance.
  - PASS: `./scripts/verify-release-gate.sh 1.6.4`, including typecheck, 437
    concentrated tests, first-run Docker proof, public/local commands, source
    and packed manifests, packed own-database proposal/apply, content checks,
    npm publish dry-run, and `git diff --check`.
  - PASS: final packed Community Solar clean room in 37.471s. Schema summary
    11.800s, first scoped read 13.671s, PM aggregate 14.305s, protected
    capability 17.992s, proposal 19.137s, guarded apply 28.595s; one command,
    zero file edits.
  - PASS: final independent packed Retail clean room in 46.747s. Schema summary
    17.055s, first scoped read 19.312s, PM aggregate 19.947s, protected
    capability 23.979s, proposal 25.142s, guarded apply 35.009s; one command,
    zero file edits.
  - PASS: human visual inspection of both final `10-human-review.png` captures
    and the highlighted Solar action preview. Review/apply content is aligned
    to the full operations workspace and the DSL tokens are visibly distinct.
  - PASS: publish dry-runs for Spec `1.7.0`, DSL `1.7.0`, scoped Runner `1.6.4`,
    and the optional unscoped alias `1.6.4`.
  - PASS: stale-copy scans, 124-file local documentation-link verification,
    exact authorized OpenAI-key absence across 1,479 files, generic added-line
    secret scan (one intentional session-only test canary), README byte
    identity/word budget, and final diff whitespace checks.

## Resume

1. Read this file and the current goal/bug/results.
2. Run `git status --short --branch`.
3. Inspect the final repository-wide diff, generated-copy/stale-doc scans, and
   secret scans. All required source, packed, browser, live-database, fleet,
   client, release, and clean-room gates above are green.
4. Do not merge, push, publish, tag, deploy, or create a release without
   explicit owner authorization.
