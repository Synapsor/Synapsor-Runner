services:
  postgres:
    image: postgres:16
    container_name: synapsor_runner_mcp_postgres_support
    environment:
      POSTGRES_DB: synapsor_runner_mcp_support
      POSTGRES_USER: synapsor_admin
      POSTGRES_PASSWORD: synapsor_admin_password
    ports:
      - "55434:5432"
    volumes:
      - ./seed:/docker-entrypoint-initdb.d:ro
